---
label: Media + Text
hide_body: true
fields:
- name: block
  type: text
  config:
    required: false
  label: block
  hidden: true
  default: media-text
- name: media
  type: file
  config:
    maxSize: 64
  label: Media (Image or Video)
  description: Choose an image or video as your media.
- name: heading
  type: text
  config:
    required: false
  label: Heading
  description: Write a heading for your text area.
- name: content
  type: text
  config:
    required: false
  description: Add some content below your heading.
  label: Content
- name: cta
  type: field_group
  config: {}
  fields:
  - name: enabled
    type: boolean
    label: Enabled
    default: true
    description: Enable to display a CTA button. Disable to remove the button.
  - name: color
    type: select
    default: white
    config:
      required: false
      options:
      - white
      - blue
      source:
        type: simple
        section: 
        file: 
        path: 
    label: Color
    description: Select a background color for your CTA button.
  - name: button-text
    type: text
    config:
      required: false
      max: 50
    label: Button Text
    description: Text for your CTA button.
  - name: url
    type: text
    config:
      required: false
    label: URL
    description: The relative URL (e.g. https://www.example.com<strong>/another-page</strong>)
      for your CTA button.
  label: CTA Button
