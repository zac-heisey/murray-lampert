---
label: Project List
hide_body: true
fields:
- name: block
  type: text
  config:
    required: false
  label: block
  hidden: true
  default: project-list
- name: project-list
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: featured-projects
  label: Project List
  description: Choose up to 4 featured projects to display on the main projects page.
